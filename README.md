# gwr-numpy-demo
A minimal, runnable Geographically Weighted Regression example built in pure NumPy, showing local regression, kernel weighting, bandwidth selection, and basic model diagnostics on a small synthetic spatial dataset.
